export default function Placeholder({ title }) {
  return (
    <div>
      <h1 className="text-2xl font-semibold">{title}</h1>
      <p className="mt-2 text-[var(--color-muted)]">Módulo en construcción.</p>
    </div>
  );
}
